# Resolved by the BROWSER, not inside the compose network — so localhost,
# never http://backend:8000. See root docker-compose.yml.
VITE_API_URL=http://localhost:8000
VITE_STAPLE_GRAPHQL_URL=https://dash-dev.staple.io/graphql
